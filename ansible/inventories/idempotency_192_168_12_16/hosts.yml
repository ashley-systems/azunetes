---
# 冪等性確認用・一時インベントリ（192.168.12.16）
# 確認後は削除して問題ありません
all:
  hosts:
    192.168.12.16:
      ansible_user: ansible
      ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
      ansible_ssh_common_args: '-o ProxyJump=jo3qma@explorer -o StrictHostKeyChecking=accept-new -o ForwardAgent=yes'
  children:
    controlplane:
      hosts:
        192.168.12.16:
